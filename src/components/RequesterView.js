import React, { PropTypes } from 'react'

const RequesterView = React.createClass({
  render () {
    return (
      <div>
        This is a RequesterView
      </div>
    )
  }
})

export default RequesterView
