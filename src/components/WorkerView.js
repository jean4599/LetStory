import React, { PropTypes } from 'react'

const WorkerView = React.createClass({
  render () {
    return (
      <div>
        This is a WorkerView
      </div>
    )
  }
})

export default WorkerView
